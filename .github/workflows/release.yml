name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm ci

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Verify conformance
        run: bash scripts/conformance.sh

      - name: Verify README command examples
        run: bash scripts/check_readme_examples.sh

      - name: Validate schemas
        run: bash scripts/validate_schemas.sh

      - name: Verify version/tag consistency
        run: |
          TAG_NAME=""
          if [[ "${GITHUB_EVENT_NAME}" == "push" && "${GITHUB_REF_TYPE}" == "tag" ]]; then
            TAG_NAME="${GITHUB_REF_NAME}"
          fi
          bash scripts/release/verify_versions.sh "${TAG_NAME}"

  package-node:
    needs: [verify, publish-crate]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Build npm package tarball
        run: npm pack

      - name: Upload npm artifact
        uses: actions/upload-artifact@v4
        with:
          name: node-package
          path: '*.tgz'

      - name: Check npm version availability
        id: npm_package_check
        run: |
          PKG_NAME="$(node -p "require('./package.json').name")"
          PKG_VERSION="$(node -p "require('./package.json').version")"
          if npm view "${PKG_NAME}@${PKG_VERSION}" version --registry https://registry.npmjs.org >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "npm already has ${PKG_NAME}@${PKG_VERSION}; skipping publish."
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish to npm
        if: ${{ github.event_name == 'push' && github.ref_type == 'tag' && steps.npm_package_check.outputs.exists != 'true' }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --access public

  publish-github-package:
    needs: verify
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js for GitHub Packages
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://npm.pkg.github.com'

      - name: Prepare scoped package name
        env:
          REPOSITORY_OWNER: ${{ github.repository_owner }}
        run: |
          OWNER="$(echo "$REPOSITORY_OWNER" | tr '[:upper:]' '[:lower:]')"
          export OWNER
          node -e "const fs=require('fs'); const pkg=require('./package.json'); const base=pkg.name.includes('/') ? pkg.name.split('/').pop() : pkg.name; pkg.name='@'+process.env.OWNER+'/'+base; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2)+'\n');"

      - name: Check GitHub Packages version availability
        id: gh_package_check
        env:
          NODE_AUTH_TOKEN: ${{ github.token }}
        run: |
          PKG_NAME="$(node -p "require('./package.json').name")"
          PKG_VERSION="$(node -p "require('./package.json').version")"
          if npm view "${PKG_NAME}@${PKG_VERSION}" version --registry https://npm.pkg.github.com >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "GitHub Packages already has ${PKG_NAME}@${PKG_VERSION}; skipping publish."
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish to GitHub Packages
        if: ${{ steps.gh_package_check.outputs.exists != 'true' }}
        env:
          NODE_AUTH_TOKEN: ${{ github.token }}
        run: npm publish --registry https://npm.pkg.github.com

  package-rust:
    needs: verify
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: chorus-linux-x64
            binary_path: target/release/chorus
          - os: macos-latest
            artifact_name: chorus-macos-arm64
            binary_path: target/release/chorus
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build release binary
        run: cargo build --manifest-path cli/Cargo.toml --release

      - name: Upload release binary
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.binary_path }}

  publish-crate:
    needs: verify
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Publish crate to crates.io
        if: ${{ github.event_name == 'push' && github.ref_type == 'tag' }}
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: cargo publish --manifest-path cli/Cargo.toml

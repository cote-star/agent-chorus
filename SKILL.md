---
name: agent-chorus
description: Chorus CLI session context across Codex, Gemini, Claude, and Cursor for verification, steering, analysis, and feedback.
---

# Agent Chorus Skill

The Claude Code plugin skill definition lives at:

```
skills/agent-chorus/SKILL.md
```

The plugin manifest is at `.claude-plugin/plugin.json`.

For agent-specific instruction blocks injected per project by `chorus setup`, see:
- `CLAUDE.md` — Claude Code integration
- `AGENTS.md` — Codex and Gemini integration
